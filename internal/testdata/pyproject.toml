[tool.poetry]
name = "test-app"
version = "1.0.0"

[tool.poetry.dependencies]
python = "^3.9"
requests = "2.28.2"
flask = "^2.3.0"
Pillow = "9.0.0"
cryptography = "2.9.2"

[tool.poetry.dev-dependencies]
pytest = "^7.4.0"

[project]
dependencies = [
    "numpy>=1.20.0",
    "pandas==2.0.0",
]
